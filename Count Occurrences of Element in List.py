def count_occurrences(lst, element):
    return lst.count(element)

# Example usage:
my_list = [1, 2, 3, 4, 2, 2, 5, 6, 2]
element_to_count = 2

print(f"The element {element_to_count} occurs {count_occurrences(my_list, element_to_count)} times in the list.")
