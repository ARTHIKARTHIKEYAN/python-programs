# Define a string
my_string = "Hello, World!"

# Use the sum() function with map() to find the length of the string
length_of_string = sum(map(lambda x: 1, my_string))

# Print the length of the string
print("Length of the string:", length_of_string)
